# Task ID: 13
# Title: Implement Timeline UI Integration into Project Editor Page
# Status: pending
# Dependencies: 3, 4
# Priority: high
# Description: Wire up Timeline component into ProjectEditorPage to enable complete timeline editing functionality at route /projects/:projectId/editor
# Details:
Integrate Timeline component into ProjectEditorPage with complete TimelineStore connection for interactive timeline editing. Import Timeline component and useTimelineStore hook from contexts. Replace placeholder timeline div (lines 68-75) with actual Timeline component rendering. Initialize default tracks (video, audio, text) on component mount using TimelineStore.addTrack if no tracks exist. Wire up all Timeline props to TimelineStore: tracks array, clips Map, selectedClipIds array, playhead position, zoom level, duration, and fps. Connect Timeline event handlers to store actions: onPlayheadChange calls TimelineStore.setPlayhead, onZoomChange calls setZoom, onClipSelect calls selectClip, onClipMove calls moveClip, onTrackUpdate calls updateTrack, onAddTrack calls addTrack. Ensure proper layout integration maintaining preview area above and properties panel on right side. Follow React best practices from DeepWiki research: use Zustand selectors with useShallow for optimal performance, prevent unnecessary re-renders by selecting only needed state slices, ensure immutable state updates in all store actions. Verify Timeline is fully interactive at route /projects/:projectId/editor with proper store synchronization.

# Test Strategy:
Test Timeline component renders in ProjectEditorPage at route /projects/:projectId/editor, verify all timeline interactions (playhead drag, zoom, clip selection, track management) update TimelineStore correctly, validate store state changes trigger Timeline re-renders, test default tracks initialize on first mount, verify layout integration with preview and properties panels, test performance with useShallow selectors prevents unnecessary re-renders, validate immutable state updates throughout

# Subtasks:
## 1. Import Timeline component and useTimelineStore hook into ProjectEditorPage [pending]
### Dependencies: None
### Description: Add necessary imports for Timeline component and TimelineStore context hook to enable integration
### Details:
In ProjectEditorPage.tsx, add import statement for Timeline component from '../components/timeline'. Import useTimelineStore hook from '../contexts/StoreContext'. Import useEffect and useCallback hooks from React for component lifecycle and event handlers. Verify imports are correctly typed with TypeScript. Ensure no import errors and all paths resolve correctly.

## 2. Initialize default tracks using TimelineStore on component mount [pending]
### Dependencies: 13.1
### Description: Add useEffect to initialize default video, audio, and text tracks when ProjectEditorPage first mounts
### Details:
Create useEffect hook in ProjectEditorPage that runs once on mount (empty dependency array). Get timelineStore instance using useTimelineStore(). Check if timelineStore.tracks.length === 0 to determine if initialization needed. If no tracks exist, call timelineStore.addTrack three times to create default tracks: 1) Video track with type 'video', name 'Video 1', height 80, order 0, 2) Audio track with type 'audio', name 'Audio 1', height 60, order 1, 3) Text track with type 'text', name 'Text/Titles', height 60, order 2. Follow same pattern as TimelinePage.tsx for consistency. Set all tracks with locked: false, hidden: false, muted: false initially.

## 3. Replace placeholder timeline div with Timeline component [pending]
### Dependencies: 13.1, 13.2
### Description: Remove placeholder timeline UI and render actual Timeline component with proper layout structure
### Details:
In ProjectEditorPage.tsx, locate placeholder timeline div at lines 68-75. Delete entire placeholder div including parent container. Add Timeline component in same location within the timeline area container. Maintain h-64 height class on parent div for consistent layout. Keep bg-zinc-900 and border-t border-zinc-800 styling on timeline container. Remove p-4 padding since Timeline handles its own internal spacing. Ensure Timeline component is properly nested within the main editor layout structure, appearing below the preview area and properties panel.

## 4. Wire up Timeline props to TimelineStore state using selectors [pending]
### Dependencies: 13.2, 13.3
### Description: Connect all Timeline component props to TimelineStore state using optimized Zustand selectors with useShallow
### Details:
Import useShallow from 'zustand/react/shallow' for optimal selector performance. Use useTimelineStore with useShallow to select multiple state properties: const { tracks, clips, selectedClipIds, playhead, zoom, fps } = useTimelineStore(useShallow((state) => ({ tracks: state.tracks, clips: state.clips, selectedClipIds: state.selectedClipIds, playhead: state.playhead, zoom: state.zoom, fps: state.fps }))). Calculate duration: const duration = timelineStore.duration > 0 ? timelineStore.duration : timelineStore.fps * 300 (5 minutes default, matching TimelinePage pattern). Pass all props to Timeline component: tracks={tracks}, clips={clips}, selectedClipIds={selectedClipIds}, playhead={playhead}, zoom={zoom}, duration={duration}, fps={fps}. This ensures Timeline only re-renders when these specific state slices change, following DeepWiki best practices.

## 5. Connect Timeline event handlers to TimelineStore actions [pending]
### Dependencies: 13.4
### Description: Wire up all Timeline event handler props to corresponding TimelineStore actions using useCallback for performance
### Details:
Get store instance with const timelineStore = useTimelineStore(). Create event handler for adding tracks: const handleAddTrack = useCallback(() => { const trackNumber = timelineStore.tracks.length + 1; timelineStore.addTrack({ type: 'video', name: `Track ${trackNumber}`, height: 80, locked: false, hidden: false, muted: false, order: timelineStore.tracks.length }); }, [timelineStore]). Wire up Timeline event props: onPlayheadChange={timelineStore.setPlayhead}, onZoomChange={timelineStore.setZoom}, onClipSelect={timelineStore.selectClip}, onClipMove={timelineStore.moveClip}, onTrackUpdate={timelineStore.updateTrack}, onAddTrack={handleAddTrack}. Use useCallback only for complex handlers like handleAddTrack; simple store method references don't need wrapping per DeepWiki guidance.

## 6. Verify Timeline integration and route accessibility [pending]
### Dependencies: 13.1, 13.2, 13.3, 13.4, 13.5
### Description: Test complete Timeline integration in ProjectEditorPage accessible at /projects/:projectId/editor route
### Details:
Start development server and navigate to /projects/test-project-123/editor route (or any valid projectId). Verify Timeline component renders with default tracks visible. Test all interactive features: drag playhead and confirm it moves smoothly, use zoom controls and verify timeline scales correctly, click clips to select them and see selection highlight, drag clips between tracks and verify position updates, click Add Track button and confirm new track appears. Monitor React DevTools to ensure no excessive re-renders occur. Verify TimelineStore state updates reflected in Timeline UI immediately. Test browser back/forward navigation preserves timeline state. Confirm layout looks correct with preview area above, properties panel on right, and timeline at bottom.

